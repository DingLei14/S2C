torch>=2.1.0
torchvision>=0.15.0
tensorboardX>=2.6
pyyaml>=5.4
albumentations>=1.3
scikit-image>=0.19
scipy>=1.11

# Optional modules:
# SAM functionality (replace or skip if you don't use SAM-based heads)
# pip install torch==<matching version> torchvision==<matching version>  # ensure CUDA match if needed
# pip install git+https://github.com/facebookresearch/segment-anything.git

# DINOv3 / Visa-specific tools:
# pip install matplotlib scikit-learn opencv-python pillow tqdm
# Download DINOv3 checkpoints and place them under models/dinov3/pretrained_weights/

